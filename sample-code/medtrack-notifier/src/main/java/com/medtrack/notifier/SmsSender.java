package com.medtrack.notifier;

/** Sends the finished reminder text to the SMS provider (stubbed here). */
public class SmsSender {

    public boolean send(String patientId, String message) {
        return true;
    }
}
